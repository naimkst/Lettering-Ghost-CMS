# for live server
routes:
  /signup/: members/signup
  /signin/: members/signin
  /account/: members/account

# for local server
# routes:
#     /signup/: members\signup
#     /signin/: members\signin
#     /account/: members\account

collections:
  /post/:
    permalink: /{slug}/
    template: index
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
